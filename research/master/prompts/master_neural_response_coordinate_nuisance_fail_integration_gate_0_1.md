# Prompt — Neural Response Coordinate Nuisance-Invariance FAIL Integration Gate 0.1

Authorised by: `00 – MASTER – Projektplan & Status`
Assigned chat: `00 – MASTER – Projektplan & Status`
Status: READY / AWAIT NAMED GATE
Date: 2026-09-04
Dependency: `Neural Response Coordinate Nuisance-Invariance Pilot 0.1` COMPLETE / FAIL — SPECIFICATION CLASSIFICATION GAP / RESULT FROZEN

## Name

`Neural Response Coordinate Nuisance-Invariance FAIL Integration Gate 0.1`

## Purpose

Integrate the frozen nuisance-invariance pilot without repairing its null construction or classifier, and choose exactly one programme action: `GO`, `REVISE`, or `STOP` for the neural response-coordinate direction.

This is a MASTER integration/governance gate only. It must not run experiments, change N0, add a post-hoc classifier clause, reinterpret the frozen FAIL as PASS/WEAK/NULL, search another nuisance family, or promote novelty.

## Frozen evidence

Use only the canonical project state through:

`RP-012 — Neural Response Coordinate Nuisance-Invariance Result Freeze 0.1`.

Mandatory facts to preserve:

- all mandatory numerical, analytical, leakage, oracle, gauge-invariance, B3-control and regression sanity conditions passed;
- candidate response coordinate has `R2_state=1.0` on nuisance-only, latent-only and joint held-out partitions;
- minimum joint per-intervention candidate `R2=1.0`;
- candidate `J_nuis=5.596227006606825e-32`;
- naive 2D raw-parameter PCA joint `R2_state≈0` and `J_nuis=1.0`;
- gauge-aware 2D Gram-PCA joint `R2_state=1.0` and `J_nuis=2.692209973425601e-32`;
- frozen N0 joint `R2_state=0.6999999999999995`;
- PASS and WEAK fail on their N0 thresholds, while none of the explicitly enumerated NULL conditions applies;
- the frozen classifier is therefore non-total on the observed metric vector;
- APP-A classified the gate as `FAIL — SPECIFICATION CLASSIFICATION GAP`, with no retuning.

## Required evaluation dimensions

Evaluate at least:

1. **Scientific signal versus gate validity** — separate the observed predictive/gauge-invariance facts from the invalidity of the frozen scientific classifier.
2. **Null-design diagnosis** — distinguish the direct fact that N0 scores high from the structural explanation that a one-state cyclic shift mostly maps among repeated gauge-equivalent copies.
3. **Anti-retuning** — do not amend N0 or the classifier in place.
4. **Claim ceiling** — no PASS/WEAK/NULL may be retroactively assigned to the frozen nuisance pilot.
5. **Methodological recoverability** — decide whether one prospective, independently specified validation gate could test the same scientific question with a total classifier and a null defined at the response-latent/orbit level before execution.
6. **Value of information** — compare such a prospective methodological validation against parking the coordinate direction or moving to broader nonlinear/multi-step work.
7. **Programme coherence** — determine whether the neural response-coordinate direction remains worth one further controlled test after a specification FAIL.

## Required decision

Choose exactly one:

### GO

Use only if the existing frozen evidence is sufficient to advance to a separately frozen broader scientific gate without first resolving the specification-validity problem. This should be a high bar.

### REVISE

Use if the scientific direction remains viable but exactly one prospective methodological prerequisite is needed before any broader claim. The prerequisite must be separately frozen and must not overwrite or reclassify the failed nuisance pilot.

### STOP

Use if a further test would amount primarily to post-hoc repair or if the evidence no longer justifies continued investment in the coordinate direction.

## Required deliverable

Create:

`research/master/neural_response_coordinate_nuisance_fail_integration_0_1.md`

It must contain:

1. Executive decision: GO / REVISE / STOP.
2. Frozen evidence table.
3. What is scientifically observed despite the gate FAIL.
4. What cannot be claimed because classification failed.
5. Diagnosis of the N0/classifier gap, separating result from inference.
6. Updated claim ceiling.
7. Exactly one next scientific/methodological gate, or STOP.
8. Reasons for rejecting the main alternatives.
9. Rollback/branch recommendation.
10. Exact next user command.

## Forbidden actions

Do not:

- rerun the failed pilot;
- alter or replace N0 inside the frozen result;
- add a post-hoc `R_null` classifier clause to the frozen specification;
- relabel the result PASS, WEAK or NULL;
- inspect a new candidate/null/family experimentally;
- open multiple parallel next steps;
- open nonlinear/multi-step/real-data/realistic-history/LoRA/power-grid/state-preparation/literature/manuscript work during this gate;
- claim novelty.

## Final handoff

After committing the integration memo, update MASTER status and Decision & Branch Log. End with exactly one next user action.
