# Prompt — Neural Vertical Slice Go/Revise/Stop Gate 0.1

Authorised by: `00 – MASTER – Projektplan & Status`
Assigned chat: `00 – MASTER – Projektplan & Status`
Status: READY / AWAIT NAMED GATE
Date: 2026-09-04

## Name

`Neural Vertical Slice Go/Revise/Stop Gate 0.1`

## Purpose

Perform the first programme-level decision review after the frozen sequence:

1. `Prior-Art & Definitions Audit 0.1`;
2. `CORE Synergetic Sufficiency Boundary 0.1`;
3. `Neural Minimal Benchmark 0.1`;
4. `Neural Historical Reachability 0.1`;
5. `Neural Nonlinear ReLU Pilot 0.1`.

This is a governance/integration gate. It must not generate new theory, new experiments, new literature branches, new candidate models, or manuscript claims.

## Frozen evidence to use

Use only the canonical frozen files and their claim ceilings:

- `research/literature/prior_art_definitions_audit_0_1.md`
- `research/core/synergetic_sufficiency_boundary_0_1.md`
- `research/app_a/neural_minimal_benchmark_0_1.md`
- `research/app_a/neural_historical_reachability_0_1.md`
- `research/app_a/neural_nonlinear_relu_pilot_0_1.md`

Do not reinterpret any earlier PASS as novelty promotion.

## Required decision

Classify the neural vertical slice as exactly one of:

- `GO`
- `REVISE`
- `STOP`

### GO

Use `GO` only if the frozen chain is sufficiently coherent to justify a separately frozen next stage aimed at learned response/plasticity coordinates or another clearly specified predictive benchmark.

GO does not authorise that next stage by itself. MASTER must still define and freeze its specification before execution.

### REVISE

Use `REVISE` if the programme remains viable but one specific unresolved prerequisite should be addressed before learned-coordinate work (for example theorem-to-theorem prior-art subsumption, realistic nonlinear-history reachability, or a stronger null/control baseline). Select exactly one prerequisite.

### STOP

Use `STOP` if the frozen evidence no longer supports continued investment in this neural vertical slice under the current programme framing.

## Mandatory evaluation dimensions

Evaluate at least:

1. **Prior-art compatibility** — do the surviving claims remain inside the frozen novelty ceiling?
2. **Mathematical coherence** — is the CORE bridge precise enough to constrain empirical work?
3. **Exact benchmark validity** — did the linear benchmark establish the intended function-insufficiency witness without retuning?
4. **Historical feasibility** — was the exact pair reachable under the frozen history, and how artificial is that mechanism?
5. **Nonlinear persistence** — did the frozen ReLU pilot preserve the qualitative response crossing without search?
6. **Generalisation gap** — what is still missing before broader claims?
7. **Falsifiability / stop discipline** — is there a next experiment whose failure would materially weaken the programme?
8. **Value of information** — which single next gate maximises information gain while minimising branch proliferation?

## Required deliverable

Create:

`research/master/neural_vertical_slice_go_revise_stop_0_1.md`

It must contain:

1. Executive decision: GO / REVISE / STOP.
2. Frozen evidence table.
3. Strongest positive evidence.
4. Strongest negative evidence / limitations.
5. Claim ceiling after integration.
6. One single next recommended scientific gate, or STOP.
7. Explicit reasons for not choosing the main alternatives.
8. Updated rollback point and branch recommendation.
9. Exact next user command/chat handoff.

## Forbidden actions

Do not:

- run new experiments;
- search new neural parameterisations;
- retune old gates;
- reopen the prior-art audit;
- claim generic novelty;
- open learned coordinates, LoRA, power-grid, state-preparation, or manuscript work during this gate;
- create multiple parallel next steps.

## Final handoff

After committing the deliverable, update MASTER status and Decision & Branch Log. End with exactly one next user action.
