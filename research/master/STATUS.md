# STATUS — 00 – MASTER – Projektplan & Status

Current Gate: `Post-APP-A Integration / Neural Historical Reachability Authorisation 0.1`
Status: COMPLETE / WAIT FOR APP-A HISTORY
Latest canonical file: `research/master/project_status.md`
Dependencies: Neural Minimal Benchmark 0.1 COMPLETE / PASS / RESULT FROZEN
Next instruction: User returns to existing `50 – APP-A – Neuronaler Minimalbenchmark` and enters `GO`; that chat must execute only `research/master/prompts/app_a_neural_historical_reachability_0_1.md`.
STOP boundary: MASTER must not execute the historical benchmark itself, open nonlinear/learned-coordinate/LoRA/power-grid/state-preparation branches, retune any frozen specification, or begin manuscript work before APP-A returns, unless a new named MASTER gate explicitly overrides this freeze.

## Freeze state

- Governance: FROZEN v0.1
- MASTER baseline: FROZEN
- Prior-art audit: FROZEN / PASS — CLAIM-RESTRICTED
- CORE boundary: FROZEN / PASS — CLAIM-RESTRICTED / NO NOVELTY PROMOTION
- Neural Minimal Benchmark 0.1: FROZEN / PASS — RESULT FROZEN / NO NOVELTY PROMOTION
- Neural Historical Reachability 0.1 specification: FROZEN / AWAIT EXECUTION
- Latest rollback point: `RP-004 — Neural Minimal Benchmark Result Freeze 0.1`

## Current scientific state

The frozen linear neural benchmark passed all six pre-specified criteria without retuning. Two states with identical current function and matched simple norms show opposite one-step learning advantages on the frozen symmetric task pair. Analytic and autograd results match exactly in float64 for the observed components; local frozen tests report `4 passed`.

This is a feasibility result only and carries no novelty promotion.

## Claim ceiling

The project may currently state only that the exact frozen factorised-linear pair has identical current function/matched simple norms but different symmetric one-step learning responses.

The historical gate, if successful, may add only that this exact pair is reachable from a common hidden initialization under the one pre-specified auxiliary-gradient history while the main function remains fixed.

No ordinary single-head SGD reachability, nonlinear/general neural claim, learned-coordinate claim, LoRA/transformer claim, or causal-synergetics establishment is authorised.

## Branch state

- 00 – MASTER: COMPLETE post-APP-A integration / WAIT
- 10 – CORE: COMPLETE / FROZEN / WAIT
- 20 – THEORY-A: UNOPENED
- 30 – THEORY-B: UNOPENED
- 40 – THEORY-C: UNOPENED
- 50 – APP-A: READY / AWAIT GO — Neural Historical Reachability 0.1
- 60 – APP-B: UNOPENED
- 70 – APP-C: UNOPENED
- 80 – LIT: COMPLETE / FROZEN / WAIT
- 90 – MANUSCRIPT: UNOPENED

## Blocked future work

Without a new MASTER gate, do not open:

- nonlinear neural scaling;
- learned response/plasticity coordinates;
- NTK/LoRA/adapter comparisons;
- real-data or multi-step scaling;
- power-grid / ODE-discovery work;
- controlled state preparation;
- manuscript drafting.

## Active blocker

Operational only: the existing APP-A chat has not yet executed the newly frozen historical-reachability task.

## Return protocol

After APP-A completes and ends `STOP — RETURN TO MASTER`, the next MASTER command is:

`Status?`

STOP — WAIT
