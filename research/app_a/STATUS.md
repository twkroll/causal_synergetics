# STATUS — 50 – APP-A – Neuronaler Minimalbenchmark

Current Gate: `Neural Response Coordinate Nuisance-Invariance Pilot 0.1`
Status: READY / AWAIT GO
Latest canonical result: `research/app_a/neural_response_coordinate_pilot_0_1.md`
Latest frozen result decision: `WEAK — RESULT FROZEN / NO NOVELTY PROMOTION`
Dependencies:
- Neural Minimal Benchmark 0.1 COMPLETE / FROZEN
- Neural Historical Reachability 0.1 COMPLETE / FROZEN
- Neural Nonlinear ReLU Pilot 0.1 COMPLETE / FROZEN
- Neural Vertical Slice Go/Revise/Stop Gate 0.1 COMPLETE / GO / FROZEN
- Neural Response Coordinate Specification Gate 0.1 COMPLETE / FROZEN
- Neural Response Coordinate Pilot 0.1 COMPLETE / WEAK / FROZEN
- Neural Response Coordinate WEAK Integration Gate 0.1 COMPLETE / REVISE / FROZEN
- Neural Response Coordinate Nuisance-Invariance Specification Gate 0.1 COMPLETE / SPECIFICATION FROZEN
Canonical execution prompt: `research/master/prompts/app_a_neural_response_coordinate_nuisance_invariance_pilot_0_1.md`
Next instruction: Execute only the canonical nuisance-invariance pilot when the user enters `GO`.
STOP boundary: Do not alter the frozen base family, gauge subgroup, angle grid, partitions, interventions, coordinate, baselines, decoder, invariance metric, metrics, thresholds or tolerances; do not try another nuisance/coordinate/family or open nonlinear/multi-step/real-data/realistic-history/LoRA/power-grid/state-preparation/literature/manuscript work without new MASTER authorisation.

## Frozen prior response-coordinate result

`Neural Response Coordinate Pilot 0.1` remains:

**WEAK — RESULT FROZEN / NO NOVELTY PROMOTION**

- candidate aggregate held-out `R2_state=1.0`;
- B2 equal-dimensional raw PCA `R2_state=0.999883026432542`;
- candidate minus B2 `0.000116973567458323 < 0.05` frozen PASS margin;
- all validity controls passed;
- no retuning.

This result is not repaired by the new gate.

## New frozen gate

### Neural Response Coordinate Nuisance-Invariance Pilot 0.1

Specification:
`research/master/neural_response_coordinate_nuisance_invariance_specification_0_1.md`.

Execution prompt:
`research/master/prompts/app_a_neural_response_coordinate_nuisance_invariance_pilot_0_1.md`.

The nuisance is exact hidden-basis gauge symmetry:

`(U,v)->(Q(phi)U,Q(phi)v)`, with `Q(phi)=R(phi)⊕I3`.

Freeze exactly eight angles `phi_j=j*pi/4` and the deterministic 648-state four-partition design.

Current function and one-step response operator are analytically invariant under this gauge. APP-A must verify these identities and execute the full frozen predictive/invariance benchmark without modification.

Candidate: 2D response-PCA.

Principal naive comparator: B2 2D raw-parameter PCA.

Mandatory strong control: B3 2D gauge-invariant Gram-PCA from `[vech(U^TU),||v||^2]`.

Primary joint PASS requires, among all other frozen conditions, candidate `R2_state>=0.95`, candidate-B2 margin `>=0.10`, candidate `J_nuis<=1e-8`, and B2 nuisance-fraction disadvantage `>=0.05`.

B3 must remain successful as a strong symmetry-aware control. Thus even PASS cannot be interpreted as uniquely response-specific information beyond all raw-state quotient constructions.

## Claim ceiling

No nuisance-invariance result exists yet.

No response-aware superiority, useful gauge-invariant causal/plasticity coordinate, generic nonlinear scaling, realistic SGD reachability, real-data usefulness, LoRA/transformer relevance, controlled state preparation, novelty or established causal synergetics may be claimed before execution and result freeze.

## CI status

Repository CI remains not configured. APP-A must report commit status/workflow state for its execution commit and local frozen tests.

STOP — AWAIT GO
